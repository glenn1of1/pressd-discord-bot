# Pinned deliberately: Railway rebuilds on every deploy, and a floating
# discord.py can ship a breaking change without a single line changing here.
discord.py==2.7.1
aiohttp==3.14.1
aiosqlite==0.22.1
python-dotenv==1.2.2
